# 验证记录

## 本次整理发布 · 2026-09-24

- `demo/` 所有文件与已录制、已部署的原始 Demo 逐文件 SHA-256 校验一致。本次只整理仓库和说明，没有改动交互代码。
- `node --check demo/app.js` 和 `node --check demo/extensions.js` 通过。
- README 与说明文档中的相对文件链接检查通过。
- HTML 引用的本地脚本、样式资源入口检查通过。
- 发布范围不包括 `.openai` 部署配置、视频构建缓存、语音文件、个人对话或本地 Git 历史。

## 此前 Demo 操作验证

此前在本地 HTTP 服务与浏览器中验证过首屏、详情、共创页面、封面加载、收藏、评论、发布预览、规则替换、Skill 内容展示，以及案件预设对话转入可编辑草稿。390px 宽度的发现和互动页面未出现文档横向溢出。

此前已触发下载按钮，但自动化未捕获系统下载事件，因此不将“文件成功保存到用户设备”计为已验证结果。当前静态原型不覆盖真实账号、模型、多人同步或结算测试。
